name: Sync HIREME.md to totalmarkdown/hireme.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/business/HIREME.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/hireme.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/business/HIREME.md standalone/HIREME.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add HIREME.md
          git diff --cached --quiet || {
            git commit -m "sync: update HIREME.md from agent-md-specs"
            git push
          }
