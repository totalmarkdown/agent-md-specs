name: Sync AUDITTRAIL.md to totalmarkdown/audittrail.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/compliance/AUDITTRAIL.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/audittrail.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/compliance/AUDITTRAIL.md standalone/AUDITTRAIL.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add AUDITTRAIL.md
          git diff --cached --quiet || {
            git commit -m "sync: update AUDITTRAIL.md from agent-md-specs"
            git push
          }
