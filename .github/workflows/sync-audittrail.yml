name: Sync AUDITTRAIL.md to totalmarkdown/audittrail.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/compliance/AUDITTRAIL.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Push spec file to standalone repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.SYNC_PAT }}
        with:
          source-directory: 'specs/compliance'
          destination-github-username: 'totalmarkdown'
          destination-repository-name: 'audittrail.md'
          user-email: 'sync@totalmarkdown.ai'
          user-name: 'TotalMarkdown Sync'
          commit-message: 'sync: update AUDITTRAIL.md from agent-md-specs'
          target-branch: 'main'
          target-directory: '.'
