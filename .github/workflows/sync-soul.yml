name: Sync SOUL.md to totalmarkdown/soul.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/identity/SOUL.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Push spec file to standalone repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.SYNC_PAT }}
        with:
          source-directory: 'specs/identity'
          destination-github-username: 'totalmarkdown'
          destination-repository-name: 'soul.md'
          user-email: 'sync@totalmarkdown.ai'
          user-name: 'TotalMarkdown Sync'
          commit-message: 'sync: update SOUL.md from agent-md-specs'
          target-branch: 'main'
          target-directory: '.'
