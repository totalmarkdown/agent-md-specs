name: Sync SOUL.md to totalmarkdown/soul.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/identity/SOUL.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/soul.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/identity/SOUL.md standalone/SOUL.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add SOUL.md
          git diff --cached --quiet || {
            git commit -m "sync: update SOUL.md from agent-md-specs"
            git push
          }
