name: Sync WHOAMI.md to totalmarkdown/whoami.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/identity/WHOAMI.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Push spec file to standalone repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          SSH_DEPLOY_KEY: ${{ secrets.SYNC_DEPLOY_KEY }}
        with:
          source-directory: 'specs/identity'
          destination-github-username: 'totalmarkdown'
          destination-repository-name: 'whoami.md'
          user-email: 'sync@totalmarkdown.ai'
          user-name: 'TotalMarkdown Sync'
          commit-message: 'sync: update WHOAMI.md from agent-md-specs'
          target-branch: 'main'
          target-directory: '.'
