name: Sync WHOAMI.md to totalmarkdown/whoami.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/identity/WHOAMI.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/whoami.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/identity/WHOAMI.md standalone/WHOAMI.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add WHOAMI.md
          git diff --cached --quiet || {
            git commit -m "sync: update WHOAMI.md from agent-md-specs"
            git push
          }
