name: Sync CONSENT.md to totalmarkdown/consent.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/compliance/CONSENT.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/consent.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/compliance/CONSENT.md standalone/CONSENT.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add CONSENT.md
          git diff --cached --quiet || {
            git commit -m "sync: update CONSENT.md from agent-md-specs"
            git push
          }
