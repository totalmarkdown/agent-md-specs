name: Sync CONSENT.md to totalmarkdown/consent.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/compliance/CONSENT.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Push spec file to standalone repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          SSH_DEPLOY_KEY: ${{ secrets.SYNC_DEPLOY_KEY }}
        with:
          source-directory: 'specs/compliance'
          destination-github-username: 'totalmarkdown'
          destination-repository-name: 'consent.md'
          user-email: 'sync@totalmarkdown.ai'
          user-name: 'TotalMarkdown Sync'
          commit-message: 'sync: update CONSENT.md from agent-md-specs'
          target-branch: 'main'
          target-directory: '.'
