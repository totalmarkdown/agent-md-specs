name: Sync DELEGATION.md to totalmarkdown/delegation.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/governance/DELEGATION.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Push spec file to standalone repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.SYNC_PAT }}
        with:
          source-directory: 'specs/governance'
          destination-github-username: 'totalmarkdown'
          destination-repository-name: 'delegation.md'
          user-email: 'sync@totalmarkdown.ai'
          user-name: 'TotalMarkdown Sync'
          commit-message: 'sync: update DELEGATION.md from agent-md-specs'
          target-branch: 'main'
          target-directory: '.'
