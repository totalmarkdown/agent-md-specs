name: Sync DELEGATION.md to totalmarkdown/delegation.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/governance/DELEGATION.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/delegation.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/governance/DELEGATION.md standalone/DELEGATION.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add DELEGATION.md
          git diff --cached --quiet || {
            git commit -m "sync: update DELEGATION.md from agent-md-specs"
            git push
          }
