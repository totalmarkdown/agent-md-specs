name: Sync LIMITS.md to totalmarkdown/limits.md

on:
  push:
    branches:
      - main
    paths:
      - 'specs/governance/LIMITS.md'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-md-specs
        uses: actions/checkout@v4

      - name: Checkout standalone repo
        uses: actions/checkout@v4
        with:
          repository: totalmarkdown/limits.md
          token: ${{ secrets.SYNC_PAT }}
          path: standalone

      - name: Copy spec file
        run: cp specs/governance/LIMITS.md standalone/LIMITS.md

      - name: Push if changed
        run: |
          cd standalone
          git config user.name "TotalMarkdown Sync"
          git config user.email "sync@totalmarkdown.ai"
          git add LIMITS.md
          git diff --cached --quiet || {
            git commit -m "sync: update LIMITS.md from agent-md-specs"
            git push
          }
